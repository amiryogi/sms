# Frontend Rules

- React SPA with role-based dashboards
- Use existing layout and routing patterns
- All API calls go through service files
- No business logic in components
- UI permissions must mirror backend RBAC

Finance UI:

- Admin manages fees
- Student/Parent view only
